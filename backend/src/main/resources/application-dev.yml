spring:
  datasource:
    url: jdbc:mysql://localhost:3306/system_db_dev?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: localhost
    port: 1025

